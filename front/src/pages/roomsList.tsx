import React from 'react';

const RoomsList= () => (
	<section className="section">
		<div className="container">
			<h1>Liste des salons disponibles</h1>
		</div>
	</section>
);

export default RoomsList;
